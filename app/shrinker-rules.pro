-dontobfuscate

# Uncomment for getting Binding bind() with ? { ? } must not override an existing binding.
# Comment for getting Invalid TypeToken; must specify type parameters
-keepattributes Signature,InnerClasses,EnclosingMethod